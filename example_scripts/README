This directory contains example scripts for creating grids or running cloudy. In addition to generic scripts there are also scripts specific to using particular HPCs with specific scheduler requirements (e.g. SGE/SLURM).
